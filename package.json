{
  "name": "demons",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "node --test --test-isolation=none tests/game.test.ts",
    "test:browser": "playwright test",
    "typecheck": "vue-tsc --noEmit -p .nuxt/tsconfig.app.json",
    "format": "prettier --write app tests playwright.config.ts nuxt.config.ts README.md"
  },
  "dependencies": {
    "nuxt": "^4.5.2",
    "vue": "^3.5.42",
    "vue-router": "^5.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "prettier": "^3.9.8",
    "typescript": "^5.9.3",
    "vue-tsc": "^3.3.11"
  }
}
